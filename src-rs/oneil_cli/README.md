# Oneil CLI

The command-line interface for the Oneil programming language.

## Commands

Print the AST representation of a model:
```bash
cargo run -- dev print-ast path/to/model.on
```

Print the IR representation of a model:
```bash
cargo run -- dev print-ir path/to/model.on
```
