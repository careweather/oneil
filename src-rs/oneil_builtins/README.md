# oneil_builtins

Standard builtin values, functions, units, and prefixes for the Oneil programming language.

This crate is the single source of truth for the `std` builtin module used by the Oneil runtime and evaluator.
